CREATE TABLE Shoe (
                      shoe_id INT AUTO_INCREMENT PRIMARY KEY,
                      product_name VARCHAR(255) NOT NULL,
                      size INT NOT NULL,
                      price INT NOT NULL
);
